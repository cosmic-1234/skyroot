{
  "name": "vikram-stratos-server",
  "version": "1.0.0",
  "description": "Vikram-Stratos Mission Ops API — Digital Twin for Vikram-1 Production",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "seed": "python db/supply_gen.py && psql -d vikram_stratos -f db/schema.sql && psql -d vikram_stratos -f db/seed.sql"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "pg": "^8.13.0"
  }
}
